services:
  flashcardapp:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:12024:8080"
    env_file: 
      - .env
  #   depends_on:
  #     - db

  # db:
  #   image: postgres:16-alpine
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
  #     start_period: 20s
  #     interval: 30s
  #     retries: 5
  #     timeout: 5s
  #   volumes:
  #     - database:/var/lib/postgresql/data
  #   env_file: 
  #     - .env

# volumes:
#   database:
